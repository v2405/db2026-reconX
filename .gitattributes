# Force LF for shell scripts — prevents Windows CRLF from breaking Docker builds
mvnw        text eol=lf
*.sh        text eol=lf

# Windows batch files stay CRLF
*.cmd       text eol=crlf
*.bat       text eol=crlf

# Everything else: normalize to LF in the repo, let git handle checkout
* text=auto
